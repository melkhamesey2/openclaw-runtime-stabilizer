# Reports

This directory is reserved for locally generated Markdown reports.

Generate the mock report with:

```bash
npm run report:mock
```

The default generated file is:

```text
reports/mock-session-store-report.md
```

Generated reports are intended for local review and incident triage notes. Do not commit private paths, raw logs, secrets, or real transcripts.
