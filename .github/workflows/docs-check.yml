name: Documentation, Test, and Read-only Verification Check

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  docs-check:
    name: Validate docs, tests, fixtures, and read-only tools
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Check required files
        shell: bash
        run: |
          set -euo pipefail
          required_files=(
            README.md
            SECURITY.md
            CONTRIBUTING.md
            NOTICE
            LICENSE
            package.json
            docs/TARGET_RUNTIME.md
            docs/EXECUTIVE_SUMMARY.md
            docs/TROUBLESHOOTING_MATRIX.md
            docs/ARCHITECTURE.md
            docs/ROLLBACK_POLICY.md
            docs/OPERATIONAL_VERIFICATION.md
            docs/REDACTION_POLICY.md
            docs/ROADMAP.md
            docs/INCIDENT_TIMELINE.md
            docs/PROJECT_STATUS.md
            docs/RELEASE_NOTES.md
            docs/REPOSITORY_MAP.md
            docs/FINAL_REPORT_AR.md
            evidence/SANITIZED_EVIDENCE_SUMMARY.md
            examples/redacted-environment.example
            examples/mock-session-store/README.md
            examples/mock-session-store/sessions.json
            examples/mock-session-store/expected-report.json
            examples/mock-session-store/transcripts/clean-session.jsonl
            examples/mock-session-store/transcripts/unreferenced-extra.jsonl
            examples/mock-session-store/transcripts/trajectory-only.trajectory.jsonl
            examples/clean-session-store/sessions.json
            examples/clean-session-store/transcripts/clean-a.jsonl
            examples/clean-session-store/transcripts/clean-b.jsonl
            examples/malformed-session-store/sessions.json
            examples/malformed-session-store/transcripts/malformed.jsonl
            examples/mock-large-session-store/sessions.json
            examples/mock-large-session-store/transcripts/large-line.jsonl
            src/README.md
            src/session-store-analyzer.mjs
            tools/README.md
            tools/verify-readonly.mjs
            tools/generate-markdown-report.mjs
            test/session-store-analyzer.test.mjs
            .github/ISSUE_TEMPLATE/safe-incident-report.md
          )

          missing=0
          for file in "${required_files[@]}"; do
            if [[ ! -s "$file" ]]; then
              echo "Missing or empty required file: $file"
              missing=1
            fi
          done

          if [[ "$missing" -ne 0 ]]; then
            exit 1
          fi

      - name: Install project metadata
        shell: bash
        run: npm install --ignore-scripts

      - name: Run tests, verifier, and report generation
        shell: bash
        run: npm run check
